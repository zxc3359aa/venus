# 请先读我 —— 怎么把「维纳斯」投给 Codex（三步，不用懂代码）

这个压缩包**本身就是一个完整项目**。你只要把它整个交给 Codex，剩下的它会按里面的规则自己做。

## 三步走
**第 1 步：解压**
解压后得到 `venus/` 文件夹。

**第 2 步：用 Codex 打开这个文件夹**
- 用 Codex CLI：在终端 `cd` 进入 `venus/`，输入 `codex` 回车。
- 用 VS Code / Codex 桌面版：直接「打开文件夹」选 `venus/`。

> Codex 会**自动读取**根目录的 `AGENTS.md` 当作项目规则——这一步你什么都不用设置。

**第 3 步：把下面这段话整段复制，粘贴给 Codex**

```
请先完整阅读 AGENTS.md 和 docs/Venus-Spec-v2.0.md 两个文件。
先运行 make test，确认 14 项测试全绿（这是基线，绿了就说明环境没问题）。
然后严格遵守 AGENTS.md 的红线与操作手册，以 src/venus/contracts.py 为唯一接口锚点，
从 M0 出发，按规格 §12 的里程碑推进：用 $add-module 依次实现 M1 到 M9。
每个模块完成后运行 make test、$compliance-gate、$contract-conformance，并用 PR 提交。
任何对外不可逆动作（发视频、回评论、投流花钱、加客户、删数据）都必须先弹审批让我确认；
C3（我的画像/语料/私域个人信息）绝对不能外发到云端模型或第三方。
写任何平台接口（飞书/抖音/巨量引擎/企业微信）之前，先用 context7 查最新官方文档再动手。
每个阶段结束，请输出「已完成 / 未完成 / 阻塞项 / 下一步」。
```

就这样。之后 Codex 会一步步实现，并在每个"危险/花钱/对外发布"动作前停下来问你要不要执行。

---

## 这个包里有什么（你不需要逐个打开）
- `AGENTS.md` —— Codex 自动读取的项目规则（红线 + 该用的工具/技能 + 操作手册）。
- `docs/Venus-Spec-v2.0.md` —— **完整工程规格**（§0–§15，唯一权威文档）。
- `src/`、`tests/`、`eval/` —— 已经跑通的 M0 代码骨架（14 项测试全绿）。
- `.codex/config.toml`、`.agents/skills/` —— Codex 的插件(MCP)与技能配置，已就绪。

## 几点放心提示
- 你**不需要会写代码**；按上面三步即可。
- 让 Codex 先跑一次 `make test` 看到「14 passed」，就证明一切就绪，可以放心往下做。
- 它每次要做"对外、花钱、删除"这类不可逆操作时，都会先向你请求确认，不会自作主张。
- 如果某个平台需要你的账号授权/密钥（如飞书、巨量引擎），Codex 会提示你，把对应值填到 `.env`（参考 `.env.example`），密钥不会写进代码。
